# ============================================================
# Git 忽略规则
#
# 分三类，判据是「提交它会不会带来危害或噪声」：
#   1. 敏感（密钥、PII）—— 提交即事故，必须忽略
#   2. 运行时产物（索引、日志、缓存）—— 提交后每次运行都产生 diff 噪声
#   3. 本地环境（虚拟环境、编辑器配置）—— 因人而异，提交只会引发冲突
#
# ⚠️ 特别注意 data/ **不在忽略之列**：它是 12 份演示语料（约 128K），
# 已确认仅含占位邮箱（如 `xxx@company.com`）与虚构公司名，无真实 PII。
# README 的快速开始依赖它开箱即用，删掉会让新用户无法跑通演示。
# ============================================================

# ---------------- 1. 敏感 ----------------
# 真实密钥。仓库只保留 .env.example 作为模板；.env 一旦进入 Git 历史
# 就无法通过删除文件挽回，只能轮换密钥。
.env
.env.*
!.env.example

# ---------------- 2. 运行时产物 ----------------
# 向量索引：重建后内容全变，提交会淹没真实改动
vector_store/

# 日志与追踪：logs/trace.jsonl 每个请求追加一行，含用户提问原文
logs/
*.log

# 长期记忆：含 USER.md 用户画像与 history.jsonl 对话原文（PII）
memory_store/

# 备份与产物：含旧版源码副本、基线快照与临时图片。
# 这些目录不参与 ruff / vulture / deptry 检查（见 pyproject.toml 的 extend_exclude）
artifacts/

# 已删除功能的源码存档（自建路由 / 自建工具 / 工单与身份模块）。
# 保留在本地作为"删之前长什么样"的凭据，但**不随仓库分发**：
# 它会让读者分不清当前是哪一版，且这些代码在 docs/history/ 里已有文字记录。
_archive/

# 测试与静态检查缓存
.pytest_cache/
.pytest_tmp/
.ruff_cache/
.mypy_cache/
.coverage
coverage.xml
htmlcov/

# Python 字节码
__pycache__/
*.py[cod]
*.egg-info/

# 本地数据库（如有）
*.sqlite3
*.db

# ⚠️ 但演示业务库必须入库：它是工具通道（3 个只读工具）的数据源，
# 缺了它新克隆的用户启动自检会报「业务库缺少表」，工具链路整条不可用。
# 内容由 scripts/seed_enterprise_db.py 生成，9 名虚构员工，无真实 PII。
!data/enterprise.db

# ---------------- 3. 本地环境 ----------------
.venv/
venv/
.python-version

# uv 锁文件：本项目实际用 .venv + requirements*.txt 管依赖（见 README 的安装说明），
# uv.lock 只是本地跑 uv 时的副产物。提交它等于出现"两套依赖事实"，
# 且 README 承诺的安装路径与它不一致，会让新用户困惑。
uv.lock

# 编辑器与系统
.vscode/
.idea/
.ipynb_checkpoints/
.DS_Store
Thumbs.db
